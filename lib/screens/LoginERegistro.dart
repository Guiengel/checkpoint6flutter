import 'package:flutter/material.dart';

class LoginERegistro extends StatelessWidget {
  const LoginERegistro({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("login"),),
    );
  }
}